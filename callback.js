const acaoCarro = execucao => {
  return execucao
}

const andar = () => console.log('o carro está andando')
const parar = () => console.log('o carro parou')

acaoCarro(andar())
acaoCarro(parar())

//-------------------------------

const FizzBuzz = (x, y) => {}
